{
  "name": "@feedback-agent/agent-core",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Deterministic LangGraph workflow state and nodes for Feedback Agent.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint:check": "eslint \"src/**/*.ts\" --max-warnings 0",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "test": "node --test"
  },
  "dependencies": {
    "@feedback-agent/contracts": "workspace:*",
    "@langchain/core": "^1.2.5",
    "@langchain/langgraph": "^1.4.9",
    "@langchain/langgraph-checkpoint": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "prettier": "^3.0.0",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.66.0"
  }
}
