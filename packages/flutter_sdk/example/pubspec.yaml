name: feedback_agent_example
description: Integration example for the Feedback Agent Flutter SDK.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  feedback_agent_flutter:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
